// Karl's GIR - Pre-Build Script
// NOTE: No longer needed - data is stored in root /data/ (not /public/data/)
// Vite only clears /public/, so user accounts in root /data/ are safe
// This script is kept for compatibility but does nothing

// Exit immediately - no backup needed
process.exit(0);

